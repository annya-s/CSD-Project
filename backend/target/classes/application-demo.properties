# Explicit local-only mode. Data disappears when the application stops.
spring.datasource.url=jdbc:h2:mem:farm;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
server.address=127.0.0.1
server.servlet.session.cookie.secure=false
